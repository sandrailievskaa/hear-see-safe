/// Localized UI copy for the voice pipeline (keep Flutter/i18n at the edge).
class VoiceUiStrings {
  const VoiceUiStrings({
    required this.commandHint,
    required this.notRecognized,
    required this.confirmWifiDisable,
    required this.sessionCancelled,
    required this.systemWifiUnavailable,
  });

  final String commandHint;
  final String notRecognized;
  final String confirmWifiDisable;
  final String sessionCancelled;
  final String systemWifiUnavailable;
}
